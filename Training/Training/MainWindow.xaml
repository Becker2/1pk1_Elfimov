<Window x:Class="Training.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Training"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" MinHeight="450" MinWidth="800" MaxHeight="1080" MaxWidth="1920">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.3*"/>
                <RowDefinition Height="0.3*"/>
                <RowDefinition Height="0.3*"/>
                <RowDefinition Height="0.3*"/>
                <RowDefinition Height="0.3*"/>
                <RowDefinition Height="0.3*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Viewbox Grid.Row="0">
                <TextBlock Text="Введите название предмета и дату"/>
            </Viewbox>
            <Viewbox Grid.Row="1">
                <TextBox Name="Exam" Width="150" Height="10" FontSize="5"/>
            </Viewbox>

            <Viewbox Grid.Row="2">
                <TextBlock Text="ФИО Студента"/>
            </Viewbox>
            <Viewbox Grid.Row="3">
                <TextBox Name="Student" Width="150" Height="10" FontSize="5"/>
            </Viewbox>
            <Viewbox Grid.Row="4">
                <TextBlock Text="Оценка"/>
            </Viewbox>
            <Viewbox Grid.Row="5">
                <TextBox MaxLength="1" PreviewTextInput="NumberValidationTextBox" x:Name="Rate" Width="150" Height="10" FontSize="5"/>
            </Viewbox>
            <Grid Grid.Row="6">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Viewbox Grid.Column="0">
                        <Button Name="Add" Content="Добавить" FontSize="5" BorderThickness="0" Click="Add_Click"/>
                    </Viewbox>
                    <Viewbox Grid.Column="1">
                        <Button Name="Delete" Content="Удалить" FontSize="5" BorderThickness="0" Click="Delete_Click"/>
                    </Viewbox>
                    <Viewbox Grid.Column="2">
                        <Button x:Name="Export" Content="Экспорт" FontSize="5" BorderThickness="0" Click="Export_Click"/>
                    </Viewbox>
                    <Viewbox Grid.Column="3">
                        <Button Name="Import" Content="Импорт" FontSize="5" BorderThickness="0" Click="Import_Click"/>
                    </Viewbox>
                </Grid>
            </Grid>
        </Grid>
        <ListBox x:Name="inf" BorderThickness="0" Grid.Column="1"/>
    </Grid>
</Window>
